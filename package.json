{
  "name": "project-sybil",
  "version": "1.0.0",
  "private": true,
  "description": "Production-grade internal intelligence platform — maps organizational dependencies as a living mathematical graph",
  "workspaces": [
    "packages/ingestion",
    "packages/dashboard"
  ],
  "scripts": {
    "dev": "npm run dev --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "author": "Project Sybil Team",
  "license": "UNLICENSED"
}
